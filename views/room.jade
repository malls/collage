extends layout

block content
  script(src='/javascripts/fun.js')
  script(src='/siofu/client.js')
  body
    #zone
    #navbar
      ul
        li#baritem upload an image:
        li#baritem
          label Upload File:
          input(type="file" id="file_input")
          //- form(method="POST" action="/file-upload" enctype="multipart/form-data")
          //-   input(type="file" name="file")
          //-   input(type="submit" value="submit")
        li#baritem
          #imgzone add img from url:
            input#imgInput(type="text" name="url" placeholder="paste a url")
            button#imgbutton submit 
        li#baritem change the background:
        li#baritem
          #bgzone
            input#bgInput(type="text" name="url" placeholder="paste a url")
            button#bgbutton submit 